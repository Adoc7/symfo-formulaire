{# Ce template n'hérite de personne,
   tout comme le template inclus avec {{ include() }}. #}

<ul class="nav nav-pills nav-stacked">
    {% for formulaire in listFormulaires %}
    <li>
        <a href="{{ path('jg_platform_view', {'id' : formulaire.id}) }}">
            {{ formulaire.title }}
         </a>
    </li>
    {% endfor %}
</ul>

